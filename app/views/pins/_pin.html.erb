<div class="box">
              <%= image_tag pin.image %>
              <%= link_to pin.description, pin %>
              <% if current_user == pin.user %>
              by <%= link_to pin.user.name, root_path %>
              <% else %>
              by <%= link_to pin.user.name, pin.user %>
              <% end %>
              <% if current_user == pin.user %>
              <%# link_to 'Edit', edit_pin_path(pin), class: "btn btn-info" %>

             <%# link_to 'Destroy', pin, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
              <% end %>
</div>
            
